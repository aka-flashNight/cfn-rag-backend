# 2026-09-05

## v3 重构开发手册（docs/v3-developer/，11 份）

基于 docs/v3-searching 全部调研 + 游戏项目立绘 manifest 指南完成。核心决策：
- D1 废 LangGraph/legacy，自研 asyncio TurnOrchestrator（单一路径、流式优先）
- D2 聊天主 Agent 一次调用多产出：首行 meta JSON（情绪/好感/委派 act），其余正文；不用 function calling 做决策
- D3 后台子 Agent fire-and-steer（TaskRunner/SearchRunner），中间结果只说一次、不报数字
- D5 LLMClient：模型 Profile 注册表（思考参数/视觉）+ 参数报错降级链（剥思考参数→剥全部非标准→去图）
- D6 嵌入换 ONNX Runtime int8（模型不换 bge-small-zh-v1.5），向量库存 .npy 二进制，hybrid BM25+RRF 进生产，单次嵌入+全库矩阵乘+池内过滤
- D7 校验聚合报错+自动修复（±10% 价值/clamp）+后端兜底草案，杜绝卡死
- D8 依赖大裁剪（删 torch/langgraph/llama-index/redis 等），exe 目标 ≤160MB
- D9 立绘改读游戏 manifest（launcher/web/assets/dialogue-portraits），废弃 illustration.zip 与 ffdec；npc_state_db.json 增可选 appearance 字段
- D10 gamebridge 仅留接口 stub
- 平台推荐：火山引擎首选（50万token/模型+支持工具），魔搭建最后（不支持 tools 只能聊天）；P9 实施时需联网核实最新免费政策

注意：用户明确只看调研文档、最小化读码；联网信息缺口（GLM reasoning_effort 参数名、DeepSeek vision 思考差异）已在手册中用降级链兜底并标注。

## 分窗口 prompt 外包给 flash 模型
- 交付 docs/v3-developer/开发prompts.md：4 个串行窗口（①基础设施 ②编排与任务管线 ③立绘与打包 ④文档与回归），每段 prompt 自包含（必读文档清单+任务清单+验收标准+交接约定）。
- 执行顺序 ①→②→③→④，窗口间靠代码+实施手记.md 交接。
