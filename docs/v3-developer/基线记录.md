# v3 重构基线记录（P0，2026-09-05，master @129a908）

| 指标 | 基线值 | 来源 |
|---|---|---|
| 单 exe 体积 | 357MB（CFN-RAG-v2.9.9.exe） | 实测（调研 §0.1） |
| venv 体积 | 1.5GB | 实测 |
| 嵌入模型 | bge-small-zh-v1.5，512 维，92MB（torch fp32 栈） | 实测 |
| 向量索引 | 4426 节点，索引目录 58MB（JSON 向量 50MB） | 本机实测 |
| 索引全量构建 | fp32 批量 10 ≈ 43s | 本机实测推算 |
| 单轮检索 | 12~16 次串行嵌入+检索；仅 query 编码 fp32 约 0.75~1.0s/轮 | 代码调研 C1 + 实测 |
| 单条 query 编码延迟 | fp32 中位 62.4ms / P90 91.2ms | 本机实测 |
| 单轮 LLM 调用数（纯聊天） | 2~3 次串行（supervisor + worker + dialogue） | 代码调研 B2 |
| 任务全流程 | 校验串行短路、无自动修复、可卡死 | 代码调研 D1/D2 |
| TTFT / 任务流程成功率 | 未实测（需在旧版运行环境手动测，跳过；以架构对比代替） | — |
| tiny_golden 检索 | recall@10 = 1.0（dense/bm25/hybrid 三轨） | evals 既有报告 |

重构目标见 README.md §1（exe ≤160MB、启动 ≤5s、TTFT ≤2.5s、检索 ≤150ms、构建 ≤25s）。
