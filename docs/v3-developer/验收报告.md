# CFN-RAG v3 验收报告（P9 文档与交付 / P10 回归与评估）

> 执行日期：2026-09-06（窗口④：文档与回归阶段）。环境：`v3-refactor` @ deaf2e4 之后的工作区，
> Windows 10.0.26200 / Python 3.10.11 / 全新运行时 venv（requirements.txt 15 项 + pytest/pytest-asyncio）。
> 结论先行：**自动化可验项全部通过，recall@10 红线不回退（tiny/full 三轨均 1.000）**；
> 需真模型/真机的 4 项人工验收遗留（见 §5），其中多数已有上一窗口真机冒烟背书。

---

## 1. P10 回归与评估结果

### 1.1 pytest 全量

| 项 | 结果 |
|---|---|
| `python -m pytest tests/ -q` | **170 例全部通过**（0 失败 / 0 跳过 / 1 例既有 DeprecationWarning） |
| 备注 | 手记 0.B 记录 169 例；本窗口环境为全新 venv 补装 pytest 后实测 170（上一窗口末尾提交含 1 例新增），全绿 |

### 1.2 检索层评估（红线项）

**口径**：golden 集按 v3 索引重建（`build_golden_set` 重写：语料源换 `services.retrieval.loader.load_corpus()`，
`expected_doc_ids` 直接对齐新 `Node.id`，旧 LlamaIndex uuid 映射盲区消除）；评估走
`RetrievalEngine.retrieve_for_eval`（本窗口新增的评估专用入口：单池 type/character 过滤 + 三轨打分，
**无业务阈值**——评估口径必须放开阈值，否则召回被截断）。

| 数据集 | n | dense R@10 | bm25 R@10 | hybrid_rrf R@10 | 基线（P0） | 红线 |
|---|---|---|---|---|---|---|
| tiny_golden | 5 | 1.0000 | 1.0000 | 1.0000 | 1.0（三轨） | ✅ 不回退 |
| golden_v1（full） | 130 | 1.0000 | 1.0000 | 1.0000 | —（旧集仅 tiny 且 3/5 行可评） | ✅ |
| full 附加指标 | 130 | MRR@10 0.9949 / nDCG@10 0.9962 | 0.9962 / 0.9972 | 0.9962 / 0.9972 | — | — |

报告产物：`evals/reports/retriever_20260906_102216_deaf2e4.{md,json}`（tiny）、
`retriever_20260906_102239_deaf2e4.{md,json}`（full）。

### 1.3 bench_retrieval 指标复测（对照 04 §6 / P0 基线）

| 指标 | 要求 | 本次实测 | 基线 |
|---|---|---|---|
| 索引全量构建（4180 条） | ≤25s | **19.07s** | fp32 ~43s |
| 单轮 Tier-1 检索 p90 | ≤150ms | **63.1~72.7ms**（mean 43~45ms） | 单轮 12~16 次串行、仅编码 0.75~1s |
| 向量库落盘体积 | ≤10MB | **7.5MB** | 58MB |
| 启动到可服务 | ≤5s（06 §2） | **0.56s**（向量库命中加载 0.55s） | — |

### 1.4 启动冒烟 + 错 key 路径（08 人工清单 #9 自动化验证）

`python main.py`（真实 HTTP 栈，127.0.0.1:7077）：

- `GET /api/health` → `{"status":"ok","app":"CFN-RAG Backend"}`；启动任务 0.56s 完成。
- 创建会话 → `POST /api/game/ask?stream=true` 携带**无效 api_key**（sk-invalid-smoke-test）：
  ```
  event: error
  data: {"code":"LLMAuthError","message":"Authentication Fails, Your api key: ****test is invalid","retryable":false}

  event: done
  data: {"session_id":"..."}
  ```
  error 事件 `code/message/retryable` 三字段与 09 §1 契约一致，key 已脱敏，**回合正常收尾不卡死**（清单 #9 通过）。
- 冒烟产生的会话数据已从 `memory.db` 清理。

## 2. P9 文档交付核对

| 交付物 | 状态 | 核对说明 |
|---|---|---|
| README.md 重写 | ✅ | 按 10 §1~§2 全部要点：功能特点/架构/技术栈改为 v3 事实；双 Profile 章节删除；嵌入模型写 512 维 ONNX int8；立绘章节改为游戏 manifest 体系（含 `CFN_GAME_PROJECT_DIR`、`appearance` 可选字段、无立绘降级）；Server Profile / MCP（仓库已无 mcp_server/）/ 压测 / 立绘包 / ffdec / tools 章节全删；常见问题更新（首次构建 ≤25s 且仅指纹变化时重建、模型下载仅 dev）；免费平台章节按联网核实重写（见 §4）；发布版本说明更新（exe 实测 91MB） |
| README 可复现性 | ✅ | 源码三步骤（venv → requirements → 模型获取 → .env → launcher.py）与 `.env.example`、`scripts/` 实际命令一一对应；`.env.example` 已同步 v3 默认值（deepseek-v4-flash-vision-exp）并清理 eval_rag/LangGraph 注释 |
| 09-前端适配说明 vs events.py | ✅（修订 2 处） | 逐字段核对一致（meta/content/tool_status/agent_status/system_notice/done/error + X-CFN-Version: 3.0.0 头已在 game_api.py 实现；agent ∈ task/search、phase ∈ drafting/repairing/searching/done/failed 与 merge.py 实际映射一致）。**修订**：§1 补 `keep_alive` 注释帧说明（events.py 存在而文档遗漏）；done 的 usage 注明「无统计的极端路径可省略」 |
| AGENTS.md | ✅ | 补「pytest 跑 tests/」「运行时依赖只装 requirements.txt」两条约定（10 §3） |
| data_files_overview.md | ✅ | 任务规则不变；新增 §7「v3 落地说明」：agent_tasks.json/agent_text.json 写入链路（orchestrator 同步动作 → handlers.execute_confirm_agent_task → task_tools 原子写入）与候选池/定价卡/微调新规则 |
| release_notes_v3.0.0.md | ✅ | 按 10 §4 模板：亮点（体积/一次调用/后台并行/校验微调/立绘）+ 升级注意（前端不兼容/索引重建/Server Profile 移除） |
| evals 适配 | ✅ | `evals/rag/`（Ragas 旧轨道）移除——深绑已删的 GameRAGService/llm_client/NPCManager/MemoryManager，且 ragas 不在运行时依赖；`run_all`/`build_golden_set`/`eval_retriever`/`evals/README.md` 全部对齐新引擎；`requirements-dev.txt` 清理 ragas/datasets |

## 3. evals 适配明细（代码变更）

| 文件 | 变更 |
|---|---|
| `services/retrieval/hybrid.py` | 新增 `RetrievalEngine.retrieve_for_eval(query, type_filter, character, top_k, mode)`：评估专用入口（无阈值单池 + dense/bm25/rrf 三轨），生产 `retrieve()` 不受影响 |
| `evals/runners/build_golden_set.py` | 语料源换 `load_corpus()`；faction 排除改用 `Node.character`；`expected_doc_ids` 写新 `Node.id`；行 schema 不变（tiny 5 条 / full 130 条，种子 42） |
| `evals/retriever/eval_retriever.py` | 移除 GameRAGService/ai_engine 依赖；`_ensure_engine()` 自加载/重建索引；评估走 `retrieve_for_eval` |
| `evals/runners/run_all.py` | 仅保留 retriever 轨道，`--suite rag` 移除并在 docstring 说明原因 |
| `evals/rag/`（整目录） | **删除**（git rm）：深绑旧栈的死代码，Ragas 轨道如需恢复应基于 services/orchestrator + services/llm 重建 |
| `evals/bench_retrieval.py` | tiny_golden recall 兼容新 golden（新 id 直读，旧 uuid 文本映射兜底）；character 过滤对齐 eval 口径（不回退 npc_name——修掉 world_lore/intelligence 行误匹配空集的口径 bug） |

## 4. 免费体验平台联网核实（核实日期：2026-09-06）

| 平台 | 核实结论 | 落笔位置（README） |
|---|---|---|
| **火山方舟** | 新用户开通语言模型**每模型赠 50 万 token**（约 30 天有效）；`doubao-seed-2-0-lite` 在售（如 doubao-seed-2-0-lite-260215，输入约 0.6 元/百万 token）；OpenAI 兼容、支持工具调用 → 首选体验平台 | 首选平台 + 默认备选示例 |
| **DeepSeek** | V4-Flash：平时输入 1 元/输出 2 元（缓存命中 0.02）；**2026-08-17 起高峰时段上浮**（输出最高 9 元）——README 已注明以官方价目为准 | 官方 API 表 |
| **智谱** | `glm-5.3-flash` 输入 0.8 / 输出 2.8 / 缓存命中 0.23 元；思考恒开（实测实际成本可显著高于纸面价，README 注明低档位）；免费档 `glm-4.5-flash` 仍在 | 官方 API 表 |
| **阿里百炼** | `qwen3.8-flash` 输入 0.8 / 输出 2.7 元；新用户免费额度约 90 天有效 | 官方 API 表 |
| **OpenAI** | `gpt-5.6-luna` 2026-07-30 起 $0.20 输入 / $1.20 输出（降价 80%） | 官方 API 表 |
| **ModelScope 魔搭** | 每日 2000 次免费调用**仍成立**（单模型另有上限），引入魔粒兑换；**「API 不支持工具调用」已过时——现为「工具调用支持因模型而异（仅部分模型支持）」**，故任务/讨价还价功能不保证可用，定位为零成本试聊；官方注明不适用高并发在线任务 | 独立章节 + 限制标注 |
| **Kimi** | **与手册预期相反：仍有免费赠送**——注册+个人实名送 15 元体验额度（另有千万级 token 赠送），按量计费；列为付费备选 | 独立小节 |
| **Gemini** | 免费层**已大幅缩水**（社区实测 Flash 系从 250 次/天降至约 20 次/天、限额动态化），需代理；从「推荐」降级为「备选」 | 独立小节 |
| **其他渠道** | 硅基流动（活动赠送到 2026-12-31）、腾讯云混元（100 万 token/年）、百度千帆（每模型 100 万 token/3 个月）、社区汇总项目 FreeLLM-API-KeyHub——README 列为参考并注明额度随时可能调整 | 参考小节 |

主要来源：[火山方舟体验页](https://www.volcengine.com/experience/ark)、[豆包大模型页](https://www.volcengine.com/product/doubao)、[方舟模型列表文档](https://www.volcengine.com/docs/82379/1554678)、[ModelScope 使用限制](https://modelscope.cn/docs/model-service/API-Inference/limits)、[魔搭免费资源 list](https://www.modelscope.cn/learn/1409)、[DeepSeek 官方价目](https://api-docs.deepseek.com/zh-cn/quick_start/pricing)、[智谱定价](https://bigmodel.cn/pricing)、[GLM-5.3-Flash 文档](https://docs.bigmodel.cn/cn/guide/models/vlm/glm-5.3-flash)、[百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)、[百炼新用户免费额度](https://help.aliyun.com/zh/model-studio/new-free-quota)、[OpenAI Pricing](https://developers.openai.com/api/docs/pricing)、[Kimi API 快速入门](https://platform.kimi.com/blog/posts/kimi-api-quick-start-guide)、[Gemini Rate limits](https://ai.google.dev/gemini-api/docs/rate-limits)、[2026 免费额度汇总（腾讯云社区）](https://cloud.tencent.com/developer/article/2626756)。

## 5. 对照 08 人工验收清单（10 项）

| # | 场景 | 状态 | 依据 |
|---|---|---|---|
| 1 | 纯聊天 5 轮（TTFT ≤2.5s） | ⚠️ 需真机人工 | 上窗口真机冒烟覆盖单轮（手记 0.A.4/0.B.3）；TTFT 系统复测未做 |
| 2 | 发任务全流程 | ✅（自动化部分） | pytest e2e 覆盖全链路；上窗口真机验证发布→游戏文件写入（任务 200007）；本轮 170 例回归通过 |
| 3 | 讨价还价 2 轮 | ✅（pytest）/⚠️ 真机补测 | pytest 断言 bargain_count 生效与第 3 次拒绝；真机 HTTP 实测遗留（手记 0.5 #3） |
| 4 | 岔开话题 3 轮 | ✅（pytest） | e2e 覆盖草案保留 3 回合 + 过期自动取消 |
| 5 | 搜索型提问 | ✅（pytest） | e2e 覆盖 agent_status(search) 时序与详答 |
| 6 | 非视觉模型 | ✅ | 上窗口实测 deepseek-v4-flash 纯文本回合正常（手记 0.A.2）+ pytest 覆盖无图路径 |
| 7 | 思考恒开模型（GLM-5.3-Flash） | ⚠️ 需真机人工 | 降级链 pytest 覆盖；真模型思考参数/延迟未测 |
| 8 | 不支持 tools 平台（魔搭） | ✅（pytest）/⚠️ 真机补测 | 剥工具重试降级 pytest 覆盖；魔搭真机走查未做 |
| 9 | 断网/错 key | ✅（**本轮自动化验证**） | §1.4：error 事件三字段明确 + done 收尾不卡死 |
| 10 | 游戏读取任务 | ✅（上窗口） | 真机发布任务 200007 在游戏侧可读（手记 0.B.3）；本窗口无代码变更影响该链路 |

## 6. 遗留问题

1. **Ragas 端到端评估轨道未重建**（本窗口移除）：如需 faithfulness/answer_relevancy 等生成质量指标，
   需基于 `services/orchestrator` + `services/llm` 重写 `evals/rag`（约 0.5~1 天 + Judge LLM 费用）；检索层红线不受影响。
2. **真机人工验收 4 项**：#1 TTFT 系统复测、#3 讨价还价真机 HTTP、#7 GLM-5.3-Flash 思考模型走查、
   #8 魔搭真模型走查（需各平台 key）。
3. **Full zip 实际体积未复测**：README/release_notes 写「约 200MB（以实包为准）」估算（exe 91.2MB 已实测），
   需下次打包确认。
4. **bench_retrieval 旧 golden 口径**：旧 LlamaIndex docstore 映射路径已无意义（新 golden 直读新 id），
   下次清理可删 `_build_old_id_text_map`。
5. 既有遗留（手记 0.A.5/0.B.4）：skills/task-publishing 文档下线、appearance 内容生产、
   聊天 Agent 调 prepare 依从率统计——不在本阶段范围。
