# v3 重构实施手记

> 按 08 纪律维护：每阶段完成后回写实施记录；手册与事实冲突处以调研/实测为准，并在此登记。
> 当前进度：**P0 ✅ / P1 ✅ / P2 ✅ / P6 ✅ / P3 ✅ / P4 ✅ / P5 ✅ / P7 ✅ / P8 ✅**（P8 的 requirements 拆分在早期窗口完成；P9 其余/P10 未开始）。
> 分支 `v3-refactor`，基于 master @ f17a708。

---

## 0.A 本窗口（P7 立绘与多模态 + P8 打包阶段，2026-09-06）

### 0.A.1 提交清单

| 提交 | 阶段 | 内容 |
|---|---|---|
| da22076 | P7 | services/portraits/ 三模块 + 编排层带图接入 + assets_api 立绘 manifest 化 + .gitignore 修正（tests/ 入库） |
| 07f9ad6 | P8 | build_exe.py 重写 + tools discover pkgutil 修复 + 日志落 exe 同目录 + verify_vision_image.py |
| 9a580df | P7/P8 | 测试隔离 .env 的 CFN_GAME_PROJECT_DIR + build_exe sys.path 修复 |
| （本条） | P7 | 头像接口回归原 profiles 目录（与立绘体系解耦，用户澄清后修正） |

### 0.A.2 验收数据（对照 07 §8 / 06 §5）

| 项 | 要求 | 实测 |
|---|---|---|
| pytest | 全绿 | **163 例全绿**（新增 34：portraits 17 / 编排注入 10 / assets 6 / appearance 1） |
| exe 体积 | ≤160MB | **91.2MB**（onefile；预算余量大，06 §4 的 160MB 估算偏保守） |
| vision 带图 | 图片正确进入请求体 | `scripts/verify_vision_image.py`：同 prompt 带图/无图对比，**prompt_tokens 155 vs 18（+137）**，且模型准确描述立绘内容（"橄榄绿战术上衣+防弹背心"），无图时凭名字幻觉 |
| 非 vision 模型 | 全程无图不失败 | `deepseek-v4-flash`（vision=False）请求体为纯文本，回合正常 |
| 图像不支持回退 | 400 后去图 + 不再带图 | client 降级链（P1）+ 装配层 `is_image_unsupported_model` 跳过取图，pytest 覆盖 |
| manifest 缺失 | 降级无图不报错 | 单测：provider 返回 None / assets 404 非 500 / 聊天回合正常 |
| exe 冒烟 | 双击全功能 | 启动 0.6s 就绪、聊天/发任务 SSE 正常、头像与立绘接口 200（详见 0.A.5） |
| 立绘接口 | manifest 查表裁剪 | `/api/assets/illustration/Andy Law/普通` → 470×351 PNG（= 协议示例 bounds 逐像素一致） |

### 0.A.3 手册偏差回写（后续阶段以本文为准）

1. **头像与立绘是两套体系，/avatar 不随 07 迁移**（07 §6 只提「立绘接口」；
   初版实现误把 /avatar 也切了 manifest，经用户澄清后回归原实现）：
   `/avatar` 恒走 `resources/flashswf/portraits/profiles/{npc}.png` 原文件原位置；
   仅 `/illustration/{npc}/{emotion}` 走 manifest 查表 + bounds 裁剪。
2. **vision「本会话标记」实为进程级按模型名**（07 §4 与 02 §3.2.c 均写「本会话/
   session 级」）：client.py 的 `mark_image_unsupported` 是进程内全局 set（P1 实现）。
   本地单用户多会话语义下影响可忽略，且更保守（不会反复白付失败），维持现状。
3. **探测型 Profile（vision=None）也带图**：07 §4 表格只写「vision=true」，但
   DEFAULT_PROFILE（新模型零配置）为探测型；装配层判定用 `vision is not False`
   与 client 的 `_allow_images` 口径一致——探测型带图，400 由降级链去图并标记。
4. **tools 动态发现与 PyInstaller 冲突**（06 §4 未提及）：`ToolRegistry.discover`
   原 `Path(__file__).iterdir()` 在 onefile 下 FileNotFoundError（纯 Python 模块
   在 PYZ 内不落盘）。旧版打包靠把 api/core/services 源码整体 add-data 掩盖；
   v3 修复为 pkgutil.iter_modules 优先 + 目录扫描兜底 + hiddenimports 显式枚举
   （build_exe.py `_collect_tool_modules`）。**任何用 `__file__` 扫目录的运行时
   逻辑在 onefile 下都不可用**，后续新增代码需注意。
5. **日志文件 frozen 路径**（06 §2 未指定打包形态落点）：`_MEIPASS` 是临时解压
   目录、进程退出即删，`Path(__file__)` 推导的日志路径在 exe 下不可用；改为
   frozen 时落 exe 同目录 `cfn-rag.log`。
6. **依赖版本快照**（全新 venv 只装 requirements.txt，pip check 干净）：
   fastapi 0.141.1 / uvicorn 0.52.4 / pydantic 2.13.5 / openai 3.8.0 / httpx 0.28.1 /
   onnxruntime 1.23.2 / tokenizers 0.23.2 / numpy 2.2.6 / Pillow 12.3.0 / rank-bm25 0.2.2。
7. **.gitignore 的 `test_*.py` 规则曾把整个 tests/ 套件挡在版本库外**：前几窗口
   的 129 例从未入库（`git ls-files tests/` 中 test_*.py 为 0）。已改为仅忽略
   根目录 `/test_*.py` 与 `test/`；tests/ 正式套件入 git。
8. **exe 首次运行弹 Windows 防火墙授权**（launcher 起本地 HTTP 服务触发），
   属预期现象，README/发布说明可提示用户点「允许」。

### 0.A.4 冒烟记录（真模型 deepseek-v4-flash-vision-exp + exe 实包）

- `python main.py`（全新 venv）：health OK；SSE 纯聊天 meta→content→done 正常。
- exe（E:\program 部署形态，resources 同级，双击启动）：防火墙允许 →
  7077/7080 监听（启动就绪 0.8s）→ 浏览器自动打开前端 → 首次配置（性别/
  进度/模型/API Base/Key）→ NPC 列表（头像经 /api/assets/avatar/ 批量 200，
  原 profiles 图直出）→ 进入对话（立绘展示正常）。
- **全功能走通**：聊天回合（meta+流式正文）✓ → 发任务回合（meta 坏笑+1 →
  正文 → 后台 TaskRunner 4 轮工具循环 → 达轮限走保底草案 → 汇合详细说明）
  ✓ → 「接」确认回合（confirm 参数生成 + tool_status success + 委托已发布）
  ✓ → `agent_tasks.json`/`agent_text.json` 原子写入任务 200007（13:33:13）✓。
  全程日志无图片相关报错（vision 模型带图请求被正常接受）。
- vision 验证脚本见 0.A.2 表格（+137 prompt_tokens）。
- 注意：**前端旧版 localStorage 恢复逻辑会把 API Base 拼成畸形 URL**
  （实测出现 `https://api.deepseek.com/v1https://api.deepseek.com/chat/completions`
  → 404）；重存一次配置即恢复，属旧前端 bug，P9 前端适配时根治。

### 0.A.5 遗留问题（下一窗口注意）

1. **前端 dist 为旧版**：设置页「立绘来源」文案与「补充缺失立绘」「重新生成
   全部立绘」按钮调用已删除的 `/assets/export-illustrations`（404/503）——
   按 09 前端适配清单标注移除（P9/P10 前端适配时处理）。
2. **旧前端 localStorage 恢复配置时 API Base 拼接畸形**（见 0.A.4 末条），
   重存配置可绕过；P9 前端适配根治。
3. **appearance 内容为空**：npc_state_db.json 尚无 appearance 字段数据（内容
   工作不属本次范围）；注入与保留机制已测试覆盖。
4. **exe 内 uvicorn access log 不可见**（windowed stdout→NUL），排障看
   exe 同目录 cfn-rag.log（仅 core/services 的 logging 走该文件）。
5. 既有遗留（上窗口 #2/#3/#6/#7）不变：evals 适配、golden 集重建、讨价还价
   真机联调、merge 情绪回显依赖 prompt 约束。
6. **pytest flaky 一例**：`tests/retrieval/test_engine.py::test_threshold_filters_low_scores`
   全量跑偶发失败（随机数据落在阈值边缘）、单跑稳定通过；P10 评估集重建时
   顺手固定其随机种子。

---

## 0. 上一窗口（P3/P4/P5 编排与任务管线阶段，2026-09-06）

### 0.1 提交清单

| 提交 | 阶段 | 内容 |
|---|---|---|
| 07aabf3 | P5 | agent_tools 聚合校验/自动修复/兜底 + tools async dispatch + mood 工具下线 |
| a64bd17 | P3/P4 | orchestrator 五模块 + subagents 四模块 + meta.py 丢正文修复 + e2e |
| b345665 | P9 部分 | api/game_api.py 单一路径重写 + 删 game_rag_service + 旧库迁移 + schemas 精简 |

### 0.2 验收数据（对照 03 §9 / 05 §8）

| 项 | 要求 | 实测 |
|---|---|---|
| pytest | 全绿 | **129 例全绿**（新增 34：agent_tools 15 / tools 5 / orchestrator 14） |
| 七条主路径 e2e | Fake LLM 脚本化 | 纯聊天 / 发任务（含中间结果）/ 确认 / 取消 / 讨价还价 / 岔开话题保留与过期 / 搜索 全过 |
| 聚合校验 | V1+V7+V11 一次报 3 错 | 通过（`rules == ["V1","V11","V7"]`） |
| 自动修复 | V7 ±10% / V2 / V11 | 修复后过检且 note 正确 |
| 兜底草案 | 100% 过全量校验 | 通过（`金币x25000` 落 V7 中位） |
| 中间结果规则 | 只说一次、不报数字 | e2e 断言 `count("稍待") == 1` + 无数字 |
| confirm 失败 | 丢正文 + 补救 + 真实原因 | 通过（monkeypatch 注入失败） |
| 子 Agent 取消 | task 数归零 | 通过 |
| SSE 实测 | 符合 09 §2 时序 | stub LLM 五条路径实测（见 0.4） |

### 0.3 手册偏差回写（后续阶段以本文为准）

1. **confirm 需要一次轻量 LLM 调用生成发布文本**（02 §5 说 confirm 是"<300ms 纯后端操作"，但
   `confirm_agent_task` 的 title/description/get_dialogue/finish_dialogue 参数在 meta 行协议里
   没有携带通道）。实现：orchestrator 同步动作内先做一次非流式调用（purpose="subagent"、
   max_tokens=800）生成 JSON，LLM 失败回退模板文案（`prompts.fallback_confirm_args`）。
   该调用计入 usage，不计入「聊天主 Agent ≤3 次」预算（属同步动作内部）。09 §2.2 确认回合的
   实际时序为 `meta → tool_status(running) → tool_status(success) → content → system_notice → done`。
2. **SSE 端点保持 `/api/game/ask`**（09 §1 写 `/api/ask`）：现网前端（dist/）实际按
   `/api/game/sessions`、`/api/game/ask?stream=true` 调用，为兑现「端点不变」承诺保持现有前缀。
   前端适配（09）以事件契约为准，路径无变化。
3. **orchestrator 增加了第 5 个文件 `prompts.py`**（03 §1 只列 4 个）：聊天分层 prompt 资产
   （世界观/扮演约束/输出规则/汇合指令/confirm 参数生成）体量大，单独成文件。
4. **TaskRunner 的 dispatch 分组并行是「组间串行」**：同批 tool_calls 按 pipeline 序分组，
   组内 gather 并行、组间按序（draft→update 的草案状态继承需要）。同组可并行的只有
   skills 元工具等零依赖组合。
5. **llm/meta.py 修复一处 P1 遗留 bug**：无 meta 场景下，缓冲正文事件被排在 held
   usage/finish 之后，任何「finish 即终止」的转发方会**静默丢掉无 meta 回合的全部正文**。
   已修复为正文在前。
6. **memory/store.py 补旧库迁移**：已存在的 memory.db 是 v3 前的 schema（session_task_drafts
   无 bargain_count 列），启动时 ALTER TABLE + 从旧草案 JSON 回填，读取侧剥离内部字段。
7. **task_update 无草案时降级为重拟**：模型在有草案前误发 task_update（或草案已被
   confirm/cancel 清掉）时，orchestrator 按 note 作为 direction 启动 task_draft，不再报错卡死。
8. **V7 自动修复阈值口径**：05 §3「偏差 ≤±10%」实测按「超出量/被越过的区间端点」计算
   （超上限比 range_max、低于下限比 range_min），非区间宽度。
9. **SearchRunner 无 `retrieve_fn` 时 search_knowledge 返回错误 JSON**（engine 未就绪/测试
   注入 None），模型脚本仍可作答；生产路径 assemble_context 恒注入。

### 0.4 SSE 实测序列样例（stub LLM，`python main.py` + curl）

纯聊天（09 §2.1）：
```
event: meta   data: {"emotion":"微笑","favorability_change":1,"favorability":30,"relationship_level":"熟悉"}
event: content data: {"delta":"诶，是"} × N（纯台词分块）
event: done   data: {"session_id":"...","usage":{"prompt_tokens":320,"completion_tokens":48,"total_tokens":368}}
```

发任务（09 §2.2，宽限内完成、无过渡语）：
```
meta → content(过渡话)×N → agent_status(task, drafting,「拟定委托中」)
     → agent_status(task, done) → content(草案详述+询问接不接)
     → system_notice(任务草案已拟定，等待确认) → done
```
（首轮实测还命中了修复链：draft 触发 V1 → `agent_status(task, repairing)` → 轮内未修复 →
`agent_status(task, drafting,「生成保底方案」)` → done——兜底路径在真实 HTTP 栈下工作正常。）

确认回合（09 §2.2）：
```
meta → tool_status(confirm_agent_task, running) → tool_status(confirm_agent_task, success)
     → content(确认话术)×N → system_notice(委托已发布) → done
```
（无草案时 confirm：`tool_status failed` + `system_notice(委托确认失败：当前没有待确认的委托草案)` + 补救话术正文。）

搜索（09 §2.4）：
```
meta → content(过渡)×N → agent_status(search, searching,「查资料中」)
     → agent_status(search, done) → content(详答) → done
```

### 0.5 遗留问题（下一窗口注意）

1. **assets_api.py 仅做了可启动的最小修复**（SWF 导出 503、zip 解压保留）：窗口③按
   07 的 manifest 查表体系重构时整体替换。
2. **子 Agent 工具循环内的 search_knowledge 是同步执行**（ONNX 编码 ~1ms，事件循环内
   可接受）；若未来加大检索深度需换 to_thread。
3. **讨价还价正规路径的 HTTP 实测**因 stub 脚本顺序未覆盖（confirm 清草案后 update 走了
   降级重拟路径），pytest 已覆盖（bargain_count=1 断言）；下次真机联调补一条。
4. **`/api/health` 的 `settings.app_name` 引用已修**（Settings 无该字段）——凡引用 Settings
   已删字段处会直接 500，P8 打包前建议全量 grep 一遍。
5. **确认回合多了一次 LLM 调用**（偏差 #1），TTFT 口径的验收（01 §1 ≤2.5s）不含 confirm
   回合；P8 打包前用真模型复测常规聊天与发任务的延迟即可。
6. **merge 汇合调用的情绪回显**依赖 prompt 约束「不要输出 meta 行」+ split_meta_events 兜底
   剥离；若模型仍输出 JSON 首行会被剥离不外泄，但情绪不会更新（按设计）。
7. **evals/ 旧脚本**仍引用已删模块（P10 适配）；`tests/orchestrator` 的 FakeLLM 可复用为
   evals 的执行桩。

---

## 1. 提交清单（上一窗口：基础设施阶段）

| 提交 | 阶段 | 内容 |
|---|---|---|
| 9679ffe | P0 | 按 01 §7 删除清单下线 server profile 与 LangGraph 双路径（-8024 行） |
| 659dcfa | P1 | services/llm 四模块 + core/config 精简 + export_onnx_int8.py 初版 |
| e369d72 | P6 | services/npc/manager.py + services/memory/{store,summarize}.py |
| 3ee8ab0 | P2 | services/retrieval 六模块 + 删除 ai_engine/ |
| d9a9027 | P6/P8 | core/startup.py 并行重写、main.py 瘦身、requirements 拆分 |
| c2f5977 | P2 | ONNX int8 导出完成并校验通过 |
| f2214d0 | 测试 | pytest 95 例全绿 + bench 达标 + embedder padding 优化 |
| fd37f2a / a9c14bf | 工具 | requirements-dev 补充 / int8 量化档位对比实验 |

## 2. 验收数据（对照 08 各阶段清单）

| 指标 | 要求 | 实测 |
|---|---|---|
| pytest | 全绿 | 95 例全绿（02 §6 / 04 §7 / 06 §5 三组） |
| 索引全量构建（4180 条） | ≤25s | **19.4s**（基线 fp32 ~43s） |
| 单轮 Tier-1 检索 | ≤150ms | **p90 = 74ms**（mean 47ms） |
| int8 校验余弦 | ≥0.99 | **0.9952**（最低 0.9939） |
| tiny_golden recall@10 | 不回退 | **1.000**（旧 id 文本映射 3/5 行，见遗留 #2） |
| 向量库落盘体积 | ≤10MB | **7.5MB**（基线 58MB） |
| 单条查询编码延迟 | — | 1.03ms（基线 fp32 中位 62.4ms，60x） |
| 启动到可服务 | ≤5s（06 §2） | **1.6s**（冒烟实测；向量库命中时 mmap 加载 1.2s） |
| 首次建库（后台） | — | ~25s，不阻塞服务 |
| 运行时依赖 | 无禁用包 | requirements.txt 仅 15 项（fastapi/uvicorn/pydantic/pydantic-settings/httpx/openai/aiofiles/Pillow/numpy/onnxruntime/tokenizers/rank-bm25/pypdf/python-docx/docx2txt） |
| 并发好感度 | 无丢失更新 | pytest：20 并发 +1 全部生效（修 E1） |

## 3. 手册偏差回写（重要，后续阶段以本文为准）

1. **嵌入池化方式是 CLS，不是 mean pooling**（04 §1.3 有误）。`models/bge-small-zh-v1.5/1_Pooling/config.json` 实测 `pooling_mode_cls_token: true`，与既有语料嵌入行为一致。OnnxEmbedder 按 CLS + L2 归一化实现。
2. **optimum 导出方案不可用**（06 §3.2 的 `optimum[onnxruntime]`）：optimum 2.x 已把 ONNX 集成拆出主包且与 transformers 5.x 不兼容。改用 `torch.onnx.export`（TorchScript 导出器 `dynamo=False`，需关键字传参包装 BertModel），产物等价。另 torch 2.10 的 dynamo 导出器会把权重写外部文件、dynamic_axes 有约束问题，勿用。
3. **全量 int8 量化不达标**（04 §1.1 的 ~25MB 预估基于全量量化）：词嵌入表 int8 化后余弦仅 **0.964**，过不了 ≥0.99 硬门槛。最终 `quantize_dynamic` 限定 `op_types_to_quantize=["MatMul","Gemm"]` + `per_channel=True`（嵌入表保持 fp32），余弦 0.9952，模型体积 **54.6MB**（非 25MB）。exe 体积预算在 P8 打包时需按 55MB 口径复核。
4. **量化速度无差异**：int8_full 与 int8_linear 单查延迟 1.06/1.08ms、吞吐 309/320 条/s，噪声级持平；均比 fp32 快 1.6~1.8x（低于 Vespa 2.7~3.4x，短文本小批次正常）。选型只看质量与体积。
5. **嵌入吞吐的关键优化是 padding**：tokenizer 全局 padding 到批内最长会把多数批次拖到 512 token（p50 文本仅 ~50 token）。改为 token 长度降序分批 + 手工按批内最长 padding 后，整库嵌入 55s → 19s（216 条/s）。线程数用 ORT 默认即最优（扫过 2~20，手动降核单调变慢）。
6. **语料规模 4180 条**（调研时 4426 条，resources 语料已演进）；golden 集 expected_doc_ids 是旧 LlamaIndex uuid，新索引按文本精确映射仅 3/5 行可评。
7. **LLM 降级链的重试预算**：02 §2.2 写「同一请求最多 2 次真实调用」与 §3.2 的 a→b 剥参→裸请求链（最多 3 次）存在张力。实现取：网络重试仅首个请求享有（+1），降级链按 §3.2 字面执行（参数错最多 3 次）。
8. **fingerprint 覆盖目录**：data/dialogues、data/task、docs、data/intelligence、data/stages、data/items。若后续六类语料源扩展（如 07 的立绘 manifest），需同步加入。

## 4. int8 量化档位对比实验（evals/bench_int8_compare.py，2026-09-05）

三方自洽建库（各用各的模型编码索引与查询，与生产一致）+ 7 个真实场景 + 60 条随机自检索：

- 真实场景（物品/关卡/NPC 往事/任务/情报/世界观/loading）：**三方全部命中目标**（多为 #1），无阈值穿越失败。0.964 档不会「明显匹配不上」。
- 排序漂移（vs fp32）：0.964 档 top-10 重合率均值 **0.860**（完全一致仅 18%）；0.9952 档 **0.928**（38%）。
- 结论：0.964 档可用但是「噪声更大 + 阈值余量更小」的降级选项；现役 0.9952 档维持。若 P8 打包体积吃紧，可回退 int8_full（22.8MB）——去掉 export 脚本的 `op_types_to_quantize` 参数即可，实验脚本可复跑验证。

## 5. 遗留问题与下一窗口注意事项

1. **api/game_api.py 与 services/game_rag_service.py 未动**（按计划），import 已断（预期中间态）。重写时：检索侧接 `services/retrieval`（RetrievalEngine.retrieve → format_retrieval_context + bundle.entity_items/entity_stages 供实体提示拼装）；LLM 侧接 `services/llm`；存储接 `services/memory` 与 `services/npc`。
2. **golden 集需用新索引重建**（P10）：旧 uuid 不可用；建议按新 Node id 重建 tiny/golden 集，把映射盲区消掉。
3. **evals/ 旧脚本**（eval_retriever.py / eval_rag.py / build_golden_set.py / runners）引用已删模块，待检索接口稳定后适配；`retrieve_nodes_for_eval` 单池模式对应新接口为「pool 过滤 + dense/bm25/rrf 三轨」，可在 RetrievalEngine 上加 eval 辅助入口。
4. **运行时 venv 仍装有 torch/transformers 等**（导出时装的）。P8 打包前建议按新 requirements 重建干净 venv 再出 exe；`onnxscript` 是 torch.onnx 的隐式依赖，仅 dev 需要。
5. **scripts/build_exe.py 未重写**（P8 范围）：仍引用已删模块，打包前必须重写；models/bge-small-zh-v1.5-onnx-int8/ 整目录（model.onnx 54.6MB + tokenizer）需进打包清单，models/*/model.onnx 已在 .gitignore（权重不入 git，tokenizer 小文件尚未提交，见 #6）。
6. **models/bge-small-zh-v1.5-onnx-int8/ 的 tokenizer 小文件**（~558KB）待提交 git（与旧目录「配置/tokenizer 入库、权重不入」的惯例对齐），待作者确认后执行。
7. **NPCManager 防抖落盘**在进程崩溃（非正常退出）时最多丢 5s 内的好感度变化；06 未要求更强保证，记录在案。
8. **检索 q3 变体**用 `user_query + npc_last_message` 拼接（04 §3.3 设计），与旧版「npc_last_message 单独检索」语义略有差异；golden 全量评估（P10）时留意 world/supp 池分数是否回退。

## 6. 关键接口速查（下一窗口对接用）

```python
# 检索（同步实现，编排器用 asyncio.to_thread 包）
from services.retrieval import RetrievalEngine, RetrievalInput, get_retrieval_engine
engine = get_retrieval_engine()          # startup 已就绪；engine.ready 为 False 时返回空 bundle
bundle = engine.retrieve(RetrievalInput(
    user_query=..., npc_name=..., npc_titles=[...], npc_faction=...,
    npc_last_message=..., forbidden_other_chars={"彩蛋/成员角色小写名"}))
text = format_retrieval_context(bundle, forbidden_other_chars)   # 六类 section 拼装
# bundle.entity_items / bundle.entity_stages → game_entity_prompts 拼实体提示

# LLM
from services.llm import LLMClient, ChatRequest, LLMConfig, split_meta_events, meta_prompt_block, get_profile
client = LLMClient.for_config(LLMConfig(api_key=..., api_base=..., model_name=..., proxy_url=...))
result = await client.chat(ChatRequest(messages=[...], purpose="subagent", tools=[...]))   # 非流式（子 Agent）
async for ev in client.chat_stream(req): ...   # StreamEvent(kind=content/reasoning/usage/finish)
meta, events = await split_meta_events(stream, npc_emotions)   # meta 行协议（情绪先行）

# 存储
from services.memory import get_memory_store, get_summary_worker, should_summarize
store = get_memory_store()               # 全部方法有同名 async 版
from services.npc import get_npc_manager
mgr = await get_npc_manager(); st = await mgr.update_favorability(name, delta)   # 5s 防抖落盘
```
