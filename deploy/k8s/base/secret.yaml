apiVersion: v1
kind: Secret
metadata:
  name: cfn-rag-secret
type: Opaque
stringData:
  # 开发环境默认值；生产环境由 sealed-secrets 或外部 vault 管理
  LLM_API_KEY: "your-api-key-here"
  LLM_API_BASE: "https://generativelanguage.googleapis.com/v1beta/openai/"
  LLM_MODEL_NAME: "gemini-2.5-flash"
